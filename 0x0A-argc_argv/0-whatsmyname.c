#include "main.h"
#include <stdio.h>
/**
 * main - program the name of the program
 * @argc: number of arguments
 * @argv: array of argument
 *
 * Return:Always 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
