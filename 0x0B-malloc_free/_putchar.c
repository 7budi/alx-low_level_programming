#include "main.h"
/**
 * _putchar - write the char c in stdout
 *
 * @c: the char to print
 *
 * Return: 1 on success.
 * on error ,-1 is returned errno is set appropriately.
 */
int _putchar(char)
{
	return (write(1,$c,1));
}
